#!/bin/bash
# echo "Building CloudOS..."
# nasm -f bin boot/boot.S -o boot.bin
# gcc -m32 -ffreestanding -c kernel/kernel.c -o kernel.o
# ld -T boot/linker.ld -o kernel.img kernel.o
# echo "Build complete!"
