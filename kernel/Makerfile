CC = gcc
LD = ld
CFLAGS = -m32 -ffreestanding -nostdlib

kernel.o: kernel.c memory.c scheduler.c
    $(CC) $(CFLAGS) -c kernel.c memory.c scheduler.c

kernel.img: kernel.o
    $(LD) -T linker.ld -o kernel.img kernel.o

clean:
    rm -f kernel.o kernel.img
